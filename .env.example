###############################################
# Spotify API credentials (CRA-style names)
# These work in Vite via envPrefix in vite.config.ts
###############################################
REACT_APP_SPOTIFY_CLIENT_ID=
# Optional if using Authorization Code flow via backend
REACT_APP_SPOTIFY_CLIENT_SECRET=
# Defaults to ${origin}/callback if not set
REACT_APP_REDIRECT_URI=http://localhost:3000/callback

# Optional: Vite-style variables (kept for compatibility)
# VITE_SPOTIFY_CLIENT_ID=
# VITE_SPOTIFY_CLIENT_SECRET=
# VITE_SPOTIFY_REDIRECT_URI=